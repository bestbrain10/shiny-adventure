const validatorMiddleware = require('./validator');

describe('Validator Middleware', () => {
    it('user_id must always be present', () => {

    });

    it('user_id must always be present', () => {

    });

    it('tags can be empty', () => {

    });

    it('tags cannot be more fewer 5', () => {

    });
});